import styled from 'styled-components';

const CustomerAccountWrapper = styled.div.attrs({
  className: 'customer-account-wrapper',
})`
`;

CustomerAccountWrapper.displayName = 'CustomerAccountWrapper';

export default CustomerAccountWrapper;

